language: java
install: ant deps

sudo: false

addons:
  apt:
    packages:
      - oracle-java8-installer

jdk:
  - oraclejdk8


script:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
  - echo "compiling files" 
  - mvn test -B
  - echo "running test"
  - ant test
	
